# Instalar en /etc/systemd/journald.conf.d/size-limit.conf
# Sin esto el journal crece hasta llenar la SD.
# Aplicar: sudo systemctl restart systemd-journald
[Journal]
SystemMaxUse=50M
SystemKeepFree=200M
SystemMaxFileSize=10M
Compress=yes
