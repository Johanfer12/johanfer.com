# Instalar en /etc/sysctl.d/99-swappiness.conf
# Aplicar sin reiniciar: sudo sysctl --system
#
# El swap lo gestiona dphys-swapfile (NO fstab): para reactivarlo tras tocarlo,
# sudo systemctl restart dphys-swapfile. Son ~453 MB sobre la SD.
vm.swappiness=60
vm.page-cluster=0
